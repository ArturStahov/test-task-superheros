import styled from 'styled-components';

const BlockAddRating = styled.div`
  width: max-content;
  position: absolute;
  bottom: -70px;
  left: 10px;
`;
const Title = styled.h2`
  font-size: 1.6rem;
  font-weight: 600;
  color: #b59090;
`;

export { BlockAddRating, Title };
